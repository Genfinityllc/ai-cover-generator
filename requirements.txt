# Core API
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
python-dotenv==1.0.0
requests==2.31.0

# Essential ML libraries (CPU-only for faster build)
torch==2.0.1+cpu
torchvision==0.15.2+cpu
--find-links https://download.pytorch.org/whl/torch_stable.html

# Lightweight image processing
pillow==10.1.0
numpy==1.24.3

# Supabase integration
supabase==2.0.2

# Note: Heavy GPU dependencies commented out for Railway deployment
# Uncomment when proper GPU infrastructure is available
# diffusers==0.25.0
# transformers==4.36.0
# accelerate==0.25.0
# xformers==0.0.22
# peft==0.7.1
# opencv-python-headless==4.8.1.78
# compel==2.0.2